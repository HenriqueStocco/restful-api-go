info:
  name: all
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/notes/all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
